apiVersion: v1
kind: Service
metadata:
  name: stats-service
spec:
  type: ClusterIP
  selector:
    app: stats-service
  ports:
    - port: 8080          # Service accessible port
      targetPort: 8080    # Forward to container port 8080
      protocol: TCP
