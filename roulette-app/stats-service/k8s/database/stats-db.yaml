# k8s/databases/roulette-db.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stats-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Secret
metadata:
  name: stats-db-secret
type: Opaque
stringData:
  DB_USERNAME: jf76272
  DB_PASSWORD: password
  DB_NAME: statsDB
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stats-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stats-db
  template:
    metadata:
      labels:
        app: stats-db
    spec:
      containers:
      - name: postgres
        image: postgres:15
        env:
          - name: POSTGRES_USER
            valueFrom:
              secretKeyRef:
                name: stats-db-secret
                key: DB_USERNAME
          - name: POSTGRES_PASSWORD
            valueFrom:
              secretKeyRef:
                name: stats-db-secret
                key: DB_PASSWORD
          - name: POSTGRES_DB
            valueFrom:
              secretKeyRef:
                name: stats-db-secret
                key: DB_NAME
        ports:
          - containerPort: 5432
        volumeMounts:
          - mountPath: /var/lib/postgresql/data
            name: stats-db-storage
      volumes:
        - name: stats-db-storage
          persistentVolumeClaim:
            claimName: stats-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: stats-db-service
spec:
  selector:
    app: stats-db
  ports:
    - protocol: TCP
      port: 5432      # matches DB_PORT in Spring Boot
      targetPort: 5432
